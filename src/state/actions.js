import {
  retypeError,
  getUser,
  loginUser,
  logoutUser,
  registerUser,
} from "./store";

const actions = {
  retypeError,
  getUser,
  loginUser,
  logoutUser,
  registerUser,
};

export default actions;
