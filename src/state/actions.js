import {
  inputError,
  getUser,
  loginUser,
  logoutUser,
  registerUser,
} from "./store";

const actions = {
  inputError,
  getUser,
  loginUser,
  logoutUser,
  registerUser,
};

export default actions;
